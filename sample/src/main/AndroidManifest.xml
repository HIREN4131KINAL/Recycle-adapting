<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tranetech.infocity.userbay">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.tranetech.infocity.userbay.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SignupActivity" />
        <activity android:name=".Parties_Activity" />
        <activity android:name=".Tops_page">

        </activity>
        <activity
            android:name=".Brand_Wall"
            android:label="Brand Wall"></activity>

        <activity
            android:name=".Best_verdies"
            android:windowSoftInputMode="stateHidden" />

        <activity
            android:name=".Profile_page"
            android:label="Profile"></activity>

    </application>

</manifest>
